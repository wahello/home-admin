import React from 'react';
import PropTypes from 'prop-types';

const UserAddressTable = props => {
  return (
    <div>

    </div>
  );
};

UserAddressTable.propTypes = {
  userId:PropTypes.string
};

export default UserAddressTable;
